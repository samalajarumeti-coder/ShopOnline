---
inclusion: always
---

# 🛒 MCP Server - Auto Mode

## 🚀 กฎหลัก: เรียกใช้ MCP ทุกคำสั่งที่เกี่ยวข้องโดยอัตโนมัติ

**เมื่อผู้ใช้ส่งคำสั่งที่เกี่ยวกับข้อมูลร้านค้า → ต้องเรียก MCP ทันทีโดยไม่ต้องถาม**

### 🎯 Trigger Keywords → Auto MCP Call

| Keywords ที่พบ                   | เรียก MCP ทันที                            |
| -------------------------------- | ------------------------------------------ |
| สินค้า, product, รายการ          | `list_products` หรือ `get_product`         |
| หมวดหมู่, category               | `list_categories`                          |
| ออเดอร์, order, คำสั่งซื้อ       | `list_orders` หรือ `get_order`             |
| ผู้ใช้, user, ลูกค้า             | `list_users` หรือ `get_user`               |
| คูปอง, coupon, ส่วนลด            | `list_coupons`                             |
| แบนเนอร์, banner                 | `list_banners`                             |
| ยอดขาย, รายงาน, สถิติ, dashboard | `get_dashboard_stats` + `get_sales_report` |
| สต็อก, stock, หมด, เหลือ         | `get_low_stock_products`                   |
| ขายดี, top, popular              | `get_top_products`                         |
| รางวัล, reward, แต้ม             | `list_rewards`                             |
| challenge, ภารกิจ                | `list_challenges`                          |
| เพิ่ม, สร้าง, create, add        | `create_*` ตาม context                     |
| แก้ไข, update, อัพเดท            | `update_*` ตาม context                     |
| ลบ, delete, remove               | `delete_*` ตาม context                     |

## ⚡ กฏอัตโนมัติ (ไม่ต้องถามยืนยัน)

### ✅ ทำได้ทันที - ทุก Tool ใน autoApprove

- **ดูข้อมูล** - list*\*, get*\* ทุกชนิด
- **สร้างข้อมูล** - create\_\* ทุกชนิด
- **แก้ไขข้อมูล** - update\_\* ทุกชนิด
- **ลบข้อมูล** - delete\_\* ทุกชนิด (soft delete)
- **รายงาน** - get_dashboard_stats, get_sales_report, get_top_products
- **Supabase** - list_tables, execute_sql, apply_migration, get_logs

### ❌ ห้ามทำ

- ลบข้อมูลจริง (hard delete) - ใช้ soft delete เท่านั้น
- แก้ไข auth users โดยตรง - ใช้ Supabase Dashboard
- รัน SQL ที่ DROP table

---

## 🎯 Auto-Approve Tools (ทั้งหมด 34 tools)

### Products

```
list_products     → ดูสินค้า (filter: category_id, is_flash_sale, search)
get_product       → ดูรายละเอียด (id)
create_product    → สร้างสินค้า (name*, price*, category_id*)
update_product    → แก้ไขสินค้า (id*)
delete_product    → ลบสินค้า (id*) - soft delete
update_stock      → อัพเดทสต็อก (id*, stock หรือ adjustment)
bulk_update_products → อัพเดทหลายรายการ (ids*, updates*)
```

### Categories

```
list_categories   → ดูหมวดหมู่ทั้งหมด
create_category   → สร้างหมวดหมู่ (id*, name*, name_en*, icon*)
update_category   → แก้ไขหมวดหมู่ (id*)
delete_category   → ลบหมวดหมู่ (id*) - ต้องไม่มีสินค้า
```

### Orders

```
list_orders       → ดูออเดอร์ (filter: status, user_id, date_from, date_to)
get_order         → ดูรายละเอียดออเดอร์ (id*)
update_order_status → อัพเดทสถานะ (id*, status*)
get_order_stats   → สถิติออเดอร์ (date_from, date_to)
```

### Users

```
list_users        → ดูผู้ใช้ (filter: role, search)
get_user          → ดูรายละเอียดผู้ใช้ (id*)
update_user_points → อัพเดทแต้ม (id*, points หรือ adjustment)
update_user_role  → อัพเดท role (id*, role*)
```

### Coupons

```
list_coupons      → ดูคูปอง (filter: is_active)
create_coupon     → สร้างคูปอง (code*, discount_type*, discount_value*)
update_coupon     → แก้ไขคูปอง (id*)
delete_coupon     → ลบคูปอง (id*)
```

### Banners

```
list_banners      → ดูแบนเนอร์ (filter: is_active)
create_banner     → สร้างแบนเนอร์ (image*, title*)
update_banner     → แก้ไขแบนเนอร์ (id*)
delete_banner     → ลบแบนเนอร์ (id*)
```

### Analytics

```
get_dashboard_stats    → สถิติรวม (date_from, date_to)
get_top_products       → สินค้าขายดี (limit, date_from, date_to)
get_sales_report       → รายงานยอดขาย (group_by: day/week/month)
get_low_stock_products → สินค้าสต็อกต่ำ (threshold)
```

### Loyalty

```
list_rewards      → ดูรางวัล
create_reward     → สร้างรางวัล (name*, points_required*)
list_challenges   → ดู Challenge
create_challenge  → สร้าง Challenge (name*, type*, target_value*, reward_points*)
```

---

## 🎯 ตัวอย่างคำสั่งที่ใช้บ่อย

| ต้องการ              | คำสั่ง                                                             |
| -------------------- | ------------------------------------------------------------------ |
| ดูสินค้าทั้งหมด      | `list_products`                                                    |
| ดูสินค้า Flash Sale  | `list_products { is_flash_sale: true }`                            |
| ค้นหาสินค้า          | `list_products { search: "น้ำ" }`                                  |
| สร้างสินค้า          | `create_product { name: "...", price: 25, category_id: "drinks" }` |
| อัพเดทสต็อก +50      | `update_stock { id: 1, adjustment: 50 }`                           |
| ดูออเดอร์รอดำเนินการ | `list_orders { status: "pending" }`                                |
| อัพเดทสถานะออเดอร์   | `update_order_status { id: 123, status: "delivering" }`            |
| ดูยอดขายวันนี้       | `get_dashboard_stats`                                              |
| สินค้าขายดี 5 อันดับ | `get_top_products { limit: 5 }`                                    |
| สินค้าสต็อกต่ำกว่า 5 | `get_low_stock_products { threshold: 5 }`                          |

---

## ⚠️ ข้อควรระวัง

1. **delete_product** = soft delete (is_active = false) ไม่ลบจริง
2. **delete_category** ต้องไม่มีสินค้าในหมวดหมู่
3. **update_user_points** ใช้ `adjustment` สำหรับเพิ่ม/ลด ใช้ `points` สำหรับตั้งค่าใหม่
4. **Order status flow**: pending → confirmed → preparing → delivering → completed
5. **Coupon discount_type**: `percentage` หรือ `fixed`

---

## 🔧 Troubleshooting

| ปัญหา              | แก้ไข                                              |
| ------------------ | -------------------------------------------------- |
| MCP ไม่ทำงาน       | เช็ค SUPABASE_SERVICE_ROLE_KEY ใน .env             |
| Error: Missing key | เพิ่ม Service Role Key จาก Supabase Dashboard      |
| ลบ category ไม่ได้ | ย้ายสินค้าออกจากหมวดหมู่ก่อน                       |
| สต็อกติดลบ         | ใช้ `update_stock { id, stock: 0 }` แทน adjustment |

---

## 🤖 Auto Workflows - เรียกใช้ทันทีทุกคำสั่ง

### 📌 กฎสำคัญ: ทุกคำสั่งที่ส่งมา → เรียก MCP ก่อนตอบเสมอ

**ไม่ต้องถามยืนยัน ไม่ต้องรอ → เรียก MCP ทันที**

### เมื่อผู้ใช้พูดว่า... → ทำทันที

| คำสั่ง                        | Action                                           |
| ----------------------------- | ------------------------------------------------ |
| "ดูสินค้า" / "รายการสินค้า"   | → `list_products`                                |
| "สินค้าหมด" / "สต็อกต่ำ"      | → `get_low_stock_products`                       |
| "ยอดขาย" / "รายงาน"           | → `get_dashboard_stats` + `get_sales_report`     |
| "ออเดอร์ใหม่" / "รอดำเนินการ" | → `list_orders { status: "pending" }`            |
| "สินค้าขายดี"                 | → `get_top_products`                             |
| "เพิ่มสินค้า [ชื่อ] ราคา [X]" | → `create_product`                               |
| "อัพเดทสต็อก [ID] เป็น [X]"   | → `update_stock`                                 |
| "ส่งออเดอร์ [ID]"             | → `update_order_status { status: "delivering" }` |
| "ดู database" / "ตาราง"       | → Supabase `list_tables`                         |
| "รัน SQL"                     | → Supabase `execute_sql`                         |
| "migration"                   | → Supabase `apply_migration`                     |

### Batch Operations (ทำหลายอย่างพร้อมกัน)

| คำสั่ง           | Actions                                                                                  |
| ---------------- | ---------------------------------------------------------------------------------------- |
| "สรุปวันนี้"     | → `get_dashboard_stats` + `list_orders { status: "pending" }` + `get_low_stock_products` |
| "เตรียมเปิดร้าน" | → `get_low_stock_products` + `list_orders { status: "pending" }`                         |
| "ปิดร้าน"        | → `get_dashboard_stats` + `get_sales_report { group_by: "day" }`                         |
| "สถานะร้าน"      | → `get_dashboard_stats` + `get_low_stock_products` + `get_top_products`                  |

### 🔄 Supabase Power - Auto Activate

เมื่อพบ keywords: database, postgres, auth, storage, realtime, supabase, rls, migration, sql
→ **Activate supabase-hosted power ทันที** แล้วใช้ tools ที่เหมาะสม

---

## 🔐 Security Notes

- Service Role Key bypass RLS ทั้งหมด
- ห้าม commit key ขึ้น git
- ใช้สำหรับ admin operations เท่านั้น
